---
import Hero from "@components/ui/Hero.astro";
import Layout from "@layouts/Layout.astro";
import { getSortedBooks } from "src/lib/garden";
import sections from "src/data/sections.json";
import ContentGrid from "@components/garden/GardenEntriesGrid.astro";

const books = await getSortedBooks();
---

<Layout>
  <title slot="head">Books | Alessandro Desantis</title>

  <Hero>
    <span slot="heading">Books</span>
    <span slot="subheading">
      {sections.books.description}
    </span>
  </Hero>

  <ContentGrid items={books} itemSize="S" />
</Layout>
