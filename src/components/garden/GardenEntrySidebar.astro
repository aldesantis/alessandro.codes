---
import Toc from "@components/ui/Toc.astro";
import GardenEntryLinks from "@components/garden/GardenEntryLinks.astro";
import { render } from "astro:content";
import { getEntryIndexRecord, type GardenEntry } from "src/lib/garden/entries";

export interface Props {
  entry: GardenEntry;
}

const { entry } = Astro.props;
const { headings } = await render(entry);
const { outboundLinks, inboundLinks } = getEntryIndexRecord(entry.id);
---

<div class="space-y-4">
  <slot />

  <Toc headings={headings} title={entry.data.title} />
  <GardenEntryLinks outboundLinks={outboundLinks} inboundLinks={inboundLinks} />
</div>
