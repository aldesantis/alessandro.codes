---
import Tooltip from "@components/ui/Tooltip.astro";

export interface Props {
  status: "seedling" | "budding" | "evergreen";
}

const { status } = Astro.props;

const statusLabel = {
  seedling: "Seedling",
  budding: "Budding",
  evergreen: "Evergreen",
}[status];

const statusIcon = {
  seedling: "🌱",
  budding: "🌿",
  evergreen: "🌳",
}[status];
---

<Tooltip tooltip={statusLabel} underline={false}>
  {statusIcon}
</Tooltip>
