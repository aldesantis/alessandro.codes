---
export interface Props {
  language: "en" | "it";
}

const { language } = Astro.props;
const flag = language === "en" ? "🇬🇧" : "🇮🇹";
const languageName = language === "en" ? "English" : "Italian";
---

<span>{flag} {languageName}</span>
