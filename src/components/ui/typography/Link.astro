---
import type { HTMLAttributes } from 'astro/types';

export type Props = HTMLAttributes<'a'>;

const {class: originalClass, ...props} = Astro.props;
---

<a {...props} class:list={["text-orange-400 hover:underline", originalClass]}><slot /></a>
