# Vercel uploads the repo as an archive, which does not read .gitignore.
# Keep this in sync with the build-output and cache entries there.

node_modules
dist
.astro
.vercel
.git

# Zendo's local copy of fetched source content. Large, regenerated by the
# `zendo:fetch` CLI, and never read at build time.
.garden-source

.env
.env.*
.DS_Store
